import classes from './StartingPageContent.module.css';

const StartingPageContent = () => {
  return (
    <section className={classes.starting}>
      <h2>로그인과 회원가입을 해볼까요?</h2>
    </section>
  );
};

export default StartingPageContent;
